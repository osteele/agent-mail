{
  "name": "agent-mail",
  "version": "0.1.0",
  "description": "Durable local messaging and coordination for Claude Code and Codex agents",
  "type": "module",
  "bin": {
    "agent-mail": "src/cli.ts"
  },
  "scripts": {
    "check": "biome check src/ && tsc --noEmit",
    "fix": "biome check --write src/",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
